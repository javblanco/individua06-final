import { ValidarNumerosDirective } from './validar-numeros.directive';

describe('ValidarNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarNumerosDirective();
    expect(directive).toBeTruthy();
  });
});
