import { InscripcionPipe } from './inscripcion.pipe';

describe('InscripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new InscripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
